# Gist
Fireside chats with LLMs like GPT and his friends!


This notebook allows iterative development by exposing the app as a Streamlit based webapp that can be accessed over an ngrok tunnel.

It allows the user to upload a PDF file and then uses OpenAI's LLM (GPT-3.5) to answer any question from the uploaded PDF.
